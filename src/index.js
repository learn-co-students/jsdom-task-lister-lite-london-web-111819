document.addEventListener("DOMContentLoaded", () => {

  // ensuring the whole dom content is loaded before below code runs. We can keep our script at the top of the html
  
const inputs = document.querySelector('#create-task-form')

// const selectedInput = inputs[1]

inputs.addEventListener('submit', function(e) {
  e.preventDefault()

})




});
